name: SolTracker
options:
  bundleIdPrefix: com.soltracker
targets:
  SolTracker:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: AppMain.swift
    settings:
      GENERATE_INFOPLIST_FILE: YES
